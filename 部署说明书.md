# SparkOrbit 部署说明书（本机备选）

评委验收**优先**使用公网，无需本机安装：见 [公网访问说明.md](公网访问说明.md) → **https://wikj.online**。

本文说明提交包内的 **Windows 一键本机启动**（Python + SQLite，**不依赖 Docker / MySQL / Node**）。

---

## 1. 环境要求

| 组件 | 说明 |
|------|------|
| 操作系统 | Windows 10/11（64 位） |
| Python | **3.11+**（推荐 3.12），安装时勾选 Add to PATH |
| 端口 | 本机 **8000** 未被占用 |
| 网络 | 首次若需 `pip install` 需能访问 PyPI；AI 能力需能访问对应 API |

不需要：Docker、Node.js、MySQL。

---

## 2. 一键启动（Windows）

1. 解压 `17016457作品.zip` 或 `17016457源码.zip` 到任意目录  
2. 确认已安装 Python 3.11+  
3. 双击根目录 **`start.bat`**  
   - 自动创建/复用 `.venv` 并安装 `backend/requirements.txt`（首次可能数分钟）  
   - 强制使用 SQLite：`backend/sparkorbit.db`（自动 seed 演示数据）  
   - 后端托管 `frontend/dist`，浏览器打开 **http://127.0.0.1:8000**  
4. 使用下方演示账号登录  

停止：双击 **`stop.bat`**，或关闭标题为 `SparkOrbit` 的黑色窗口。

---

## 3. 演示账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 学生 | `student001` | `123456` |
| 教师 | `teacher001` | `123456` |
| 管理员 | `admin001` | `123456` |

---

## 4. 访问与健康检查

| 入口 | 地址 |
|------|------|
| 前端 + API（本机） | http://127.0.0.1:8000 |
| 健康检查 | http://127.0.0.1:8000/api/health |
| OpenAPI | http://127.0.0.1:8000/docs |
| 公网（推荐） | https://wikj.online |

---

## 5. API Key（可选）

密钥写在 `backend/.env`（提交包内通常已预置）。至少建议保留：

```env
DEEPSEEK_API_KEY=你的密钥
```

未配置时：可登录与浏览界面；依赖大模型的生成、Tutor 等能力不可用或降级。  
修改 `.env` 后重新运行 `start.bat`。

> 本机启动脚本会设置 `DATABASE_URL=sqlite+aiosqlite:///./sparkorbit.db`，覆盖 `.env` 中的 MySQL 地址，避免评委本机无 MySQL 时启动失败。

---

## 6. 架构说明（本机模式）

```
浏览器 → uvicorn :8000
           ├─ /          frontend/dist（SPA）
           ├─ /api/*     FastAPI
           └─ /static/*  上传与媒体
         SQLite → backend/sparkorbit.db
```

---

## 7. 常见问题

**Q：提示未检测到 Python**  
安装 [Python 3.12](https://www.python.org/downloads/)，勾选 Add python.exe to PATH，重新打开命令行后再双击 `start.bat`。也可直接使用公网 https://wikj.online。

**Q：8000 端口被占用**  
`stop.bat` 会尝试结束占用 8000 的进程；或手动结束后重试。

**Q：依赖安装失败**  
检查网络 / 代理后重试；或手动执行：

```bat
.venv\Scripts\python.exe -m pip install -r backend\requirements.txt
```

**Q：页面空白或接口失败**  
确认 `frontend\dist\index.html` 存在；访问 http://127.0.0.1:8000/api/health 应返回 JSON。

**Q：登录后部分 AI 功能不可用**  
检查 `backend/.env` 中 `DEEPSEEK_API_KEY` 等；公网环境已配置完整能力，建议优先用 https://wikj.online 验收 AI 流程。

---

## 8. 可选：Docker 部署

仓库内仍保留 `docker-compose.yml` 供服务器/有 Docker 的环境使用，**不是**本提交包的评委主路径。公网与本机 bat 即可完成验收。
