# SparkOrbit 服务器部署速查

本机包打好后，按本文用 `SparkOrbit.pem` 登录并上传。

## 0. 两类密钥

| 文件 | 用途 | 是否打进项目包 |
|------|------|----------------|
| `C:\Users\咸\Downloads\SparkOrbit.pem` | SSH 登录服务器 | **否**（只留本机） |
| `backend/.env`、根目录 `.env` | DeepSeek / 讯飞等 API | **是** |

---

## 1. 重装系统（腾讯云轻量）

1. 选 **使用容器镜像**（推荐 Ubuntu24.04-Docker29，或其他 `*-Docker29`）
2. **添加容器：不要填**（用本项目的 docker compose）
3. 登录凭证选 **SSH密钥**，用户名 **`ubuntu`**
4. 下拉选择与 `SparkOrbit.pem` 成对的密钥（如 SparkOrbit）
5. 防火墙放行 **TCP 22**、**TCP 80**
6. 重装完成后复制实例 **公网 IP**

---

## 2. Windows：准备 pem（只需一次）

在 PowerShell：

```powershell
icacls "C:\Users\咸\Downloads\SparkOrbit.pem" /inheritance:r
icacls "C:\Users\咸\Downloads\SparkOrbit.pem" /grant:r "$($env:USERNAME):(R)"
```

---

## 3. SSH 登录

把 `<公网IP>` 换成你的 IP：

```powershell
ssh -i "C:\Users\咸\Downloads\SparkOrbit.pem" ubuntu@<公网IP>
```

首次提示指纹时输入 `yes`。

登录后检查 Docker：

```bash
docker --version
docker compose version
```

若 `docker` 需要权限，执行后重新登录：

```bash
sudo usermod -aG docker ubuntu
# 然后退出 SSH 再连一次
```

---

## 4. 本机打包（若尚未打包）

在项目根目录：

```powershell
cd C:\Users\咸\Desktop\project
powershell -ExecutionPolicy Bypass -File .\scripts\pack-deploy.ps1
```

会生成 `sparkorbit-full-时间戳.tar.gz`（约数 GB，含 `资料/` 与密钥）。

---

## 5. 上传到服务器

本机 PowerShell（改 IP 与包名）：

```powershell
scp -i "C:\Users\咸\Downloads\SparkOrbit.pem" `
  "C:\Users\咸\Desktop\project\sparkorbit-full-XXXX.tar.gz" `
  ubuntu@<公网IP>:~/
```

---

## 6. 服务器解压并启动

SSH 登录后：

```bash
sudo mkdir -p /opt/sparkorbit
sudo tar -xzf ~/sparkorbit-full-XXXX.tar.gz -C /opt/sparkorbit
sudo chown -R ubuntu:ubuntu /opt/sparkorbit
cd /opt/sparkorbit
chmod +x start.sh stop.sh
chmod 600 .env backend/.env 2>/dev/null || true

# 若 docker 需 sudo：sudo ./start.sh 或先加入 docker 组
./start.sh
```

首次构建约 5–15 分钟。若内存不足（4GB），可临时加 swap：

```bash
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

再执行 `./start.sh`。

---

## 7. 验收

- 网站：`http://<公网IP>`
- 健康检查：`http://<公网IP>/api/health`
- 演示账号：`student001` / `teacher001` / `admin001`，密码 `123456`
- 教材静态：浏览器打开星库里的 PDF/视频，确认 `/static/materials/...` 可访问

可选：还原 SQL（密码与 `.env` 里 `MYSQL_ROOT_PASSWORD` 一致）：

```bash
cd /opt/sparkorbit
docker compose exec -T mysql mysql -uroot -p"$MYSQL_ROOT_PASSWORD" sparkorbit < sparkorbit.sql
# 或直接：
# docker compose exec -T mysql mysql -uroot -psparkorbit sparkorbit < sparkorbit.sql
```

---

## 8. 常用命令

```bash
cd /opt/sparkorbit
sudo docker compose ps
sudo docker compose logs -f backend
./stop.sh          # 或 sudo docker compose down
```

代码舱「服务端运行」依赖 sidecar 容器 `sparkorbit-codelab-runner`（随 `docker compose up` 默认启动）。确认：

```bash
sudo docker compose ps
# 应看到 sparkorbit-codelab-runner ... Up
sudo docker compose exec codelab-runner python -c "import urllib.request; print(urllib.request.urlopen('http://127.0.0.1:8091/health').read().decode())"
```

---

## 9. 加速重建（国内 apt / npm 源）

若后端卡在 `apt-get install ffmpeg`（访问 deb.debian.org 过慢），本机已改好 Dockerfile 后：

**1）构建窗口按 Ctrl+C 停掉**（或另开 SSH 不管它，改完再重建即可）

**2）本机 PowerShell 只上传两个 Dockerfile：**

```powershell
scp -i "C:\Users\咸\Downloads\SparkOrbit.pem" `
  "C:\Users\咸\Desktop\project\backend\Dockerfile" `
  ubuntu@154.8.171.156:/opt/sparkorbit/backend/Dockerfile

scp -i "C:\Users\咸\Downloads\SparkOrbit.pem" `
  "C:\Users\咸\Desktop\project\frontend\Dockerfile" `
  ubuntu@154.8.171.156:/opt/sparkorbit/frontend/Dockerfile
```

**3）服务器重建后端（清掉慢源那一层缓存）：**

```bash
cd /opt/sparkorbit
sudo docker compose build --no-cache backend
sudo docker compose up -d --build
```

**4）验收：**

```bash
sudo docker compose ps
curl -s http://127.0.0.1/api/health
```

浏览器打开：http://154.8.171.156

---

## 10. 完整库迁移（MySQL + API 密钥）

旧的 `sparkorbit.sql`（若不含 `star_assets`）不能当完整备份。应按下面从**本机当前库**重新导出再导入。

### 本机导出

```powershell
cd C:\Users\咸\Desktop\project
powershell -ExecutionPolicy Bypass -File .\scripts\backup_data.ps1 -MysqlBin "D:\mysql-8.1.0-winx64\bin"
# 自检：Count 必须 > 0
Select-String -Path .\backups\sparkorbit_*\mysql\sparkorbit.sql -Pattern "star_assets" | Measure-Object
Copy-Item .\backups\sparkorbit_*\mysql\sparkorbit.sql .\sparkorbit.sql -Force
```

### 上传

```powershell
scp -i "C:\Users\咸\Downloads\SparkOrbit.pem" .\sparkorbit.sql ubuntu@154.8.171.156:/opt/sparkorbit/sparkorbit.sql
scp -i "C:\Users\咸\Downloads\SparkOrbit.pem" .\backend\.env ubuntu@154.8.171.156:/opt/sparkorbit/backend\.env
scp -i "C:\Users\咸\Downloads\SparkOrbit.pem" .\.env ubuntu@154.8.171.156:/opt/sparkorbit\.env
```

### 服务器导入（容器 MySQL 密码一般为 sparkorbit）

```bash
cd /opt/sparkorbit
sudo docker compose exec -T mysql mysql -uroot -psparkorbit -e 'DROP DATABASE IF EXISTS sparkorbit; CREATE DATABASE sparkorbit CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;'
cat sparkorbit.sql | sudo docker compose exec -T mysql mysql -uroot -psparkorbit --default-character-set=utf8mb4 sparkorbit
sudo docker compose up -d --force-recreate backend
# 重建后把本机已上传的 Chroma 模型再拷进容器（路径若仍存在）
sudo docker cp ~/all-MiniLM-L6-v2 sparkorbit-backend:/root/.cache/chroma/onnx_models/
```

### 验收

```bash
sudo docker compose exec -T mysql mysql -uroot -psparkorbit -e 'SELECT COUNT(*) FROM sparkorbit.star_assets;'
curl -s http://127.0.0.1/api/health
curl -s http://127.0.0.1/api/public/health-capabilities
```

浏览器强制刷新：http://154.8.171.156  
`资料/` 已在服务器时，`file_url` 为 `/static/materials/...` 的资源可直接打开。

---

## 11. HTTPS（wikj.online 腾讯云证书）

证书文件放在 `/opt/sparkorbit/certs/`：

- `wikj.online_bundle.crt`
- `wikj.online.key`

轻量防火墙放行 **TCP 443**。访问：**https://wikj.online**（HTTP 会 301 跳转）。

摄像头/视频预览/虚拟人需在 HTTPS 下使用。

---

## 12. 访问统计（Umami Cloud）

**不要**在本机 Docker 自建 Umami，也**不要**在页面里写 `http://IP:3000/umami.js`（HTTPS 站点会 Mixed Content 被浏览器拦截，还曾导致有人覆盖生产 `index.html` 成源码入口 `/src/main.ts`）。

正确做法：

1. 打开 https://cloud.umami.is 注册，添加网站（域名 `wikj.online` / `www.wikj.online`）
2. 在本机 [`frontend/index.html`](frontend/index.html) 的 `<head>` 加入（仅 HTTPS）：

```html
<script defer src="https://cloud.umami.is/script.js" data-website-id="54e3bc8a-93b4-46f9-be92-73654d7e3705"></script>
```

3. 上传该文件到服务器后重建前端：

```powershell
scp -i "C:\Users\咸\Downloads\SparkOrbit.pem" `
  "C:\Users\咸\Desktop\project\frontend\index.html" `
  ubuntu@154.8.171.156:/opt/sparkorbit/frontend/index.html
```

```bash
cd /opt/sparkorbit
sudo docker compose build frontend && sudo docker compose up -d frontend
```

4. 浏览器打开 https://www.wikj.online ，F12 → Network 应看到对 `cloud.umami.is` 的请求；控制台几分钟内出现 PV。

若曾装过自建 Umami：`cd /opt/umami && sudo docker compose down`（或删除 `umami-*` 容器），勿再开放 3000 端口。
